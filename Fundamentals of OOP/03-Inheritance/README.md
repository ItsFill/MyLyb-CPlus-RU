========================================================================================================================

                                             📚 **Наследование классов**

========================================================================================================================

**1. ЧТО ТАКОЕ НАСЛЕДОВАНИЕ КЛАССОВ?**

------------------------------------------------------------------------------------------------------------------------

● **Наследование классов** — это механизм, при котором один класс (наследник) получает свойства и методы другого класса (родителя).

* **Родитель** — это класс, в котором прописаны общие поля и методы, подходящие всем производным классам.
* **Наследник** — это класс, который расширяет базовый класс уникальными полями и методами, характерными только для него.

**Пример:**

```cpp
class Computer {
protected:
    std::string brand;
    int ram;

public:
    void setBasicInfo(std::string b, int r) {
        brand = b;
        ram = r;
    }

    void powerOn() {
        std::cout << brand << " is starting..." << std::endl;
    }
};

class Laptop : public Computer {
protected:
    double batteryLife;

public:
    void setBattery(double b) {
        batteryLife = b;
    }

    void openLid() {
        std::cout << "Laptop lid is open. Battery: " << batteryLife << "h" << std::endl;
    }
};
