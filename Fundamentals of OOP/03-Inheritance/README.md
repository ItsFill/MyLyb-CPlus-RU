========================================================================================================================

                                             📚 **Наследование классов**

========================================================================================================================

**1. ЧТО ТАКОЕ НАСЛЕДОВАНИЕ КЛАССОВ?**

------------------------------------------------------------------------------------------------------------------------

● Наследование классов — это когда у нас есть два или более класса: родитель и наследник(и):
  - Родитель — это класс, в котором прописаны поля и методы, соответствующие всем наследникам.
  - Наследник — это класс или классы, в которых прописаны поля и методы, характерные именно для этого класса.

**Пример:**

```cpp
class Computer {
protected:
    std::string brand;
    int ram;

public:
    void setBasicInfo(std::string b, int r) {
        brand = b;
        ram = r;
    }

    void powerOn() {
        std::cout << brand << " is starting..." << std::endl;
    }
};

class Laptop : public Computer {
protected:
    double batteryLife;

public:
    void setBattery(double b) {
        batteryLife = b;
    }

    void openLid() {
        std::cout << "Laptop lid is open. Battery: " << batteryLife << "h" << std::endl;
    }
};
```
● Класс родитель — в данном случае это class Computer.

protected — означает, что мы сможем обращаться к этому полю в классах-наследниках, но не сможем в main.

Тем не менее, очень популярна практика создания всех полей private и создания в классе-родителе всех нужных методов, чтобы в случае нужды дополнять их в классах-наследниках.

● Класс наследник — class Laptop : public Computer.

: public Computer — означает, что какой-либо класс наследует всё, что находится в полях public и protected внутрь себя.

Поля private тоже наследуются, но не могут вызываться напрямую. В данном случае у класса ноутбук есть своя характеристика — батарея и методы для работы с ней.
