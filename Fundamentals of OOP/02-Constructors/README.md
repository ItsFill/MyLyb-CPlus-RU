============================================================================================

# 📚 КОНСТРУКТОРЫ В ООП (C++)

============================================================================================

**1. КОНСТРУКТОРЫ**

---

● **Конструктор состоит из имени класса.**

● **Существует 5 основных видов конструкторов:**

* **Конструктор по умолчанию** — ничего не принимает и не делает. Создается автоматически, если в классе нет ни одного явно прописанного конструктора. Является неявным.
* **Параметризованный конструктор** — принимает параметры и инициализирует ими поля класса.
    * *Пример (список инициализации):*
        ```cpp
        class City {
            std::string name;
        public:
            City(std::string name) : name(name) {}
        };
        ```
    * *Пример (через this):*
        ```cpp
        class City {
            std::string name;
        public:
            City(std::string name) {
                this->name = name;
            }
        };
        ```

* **Конструктор копирования** — создает новый объект как копию существующего. Принимает ссылку на объект того же класса.
    ```cpp
    class City {
        std::string name;
    public:
        City(const City& other) {
            this->name = other.name;
        }
    };
    ```

* **Конструктор переноса (move)** — «крадет» данные из временного объекта и обнуляет его, чтобы избежать дорогого копирования.
    ```cpp
    class City {
        std::string name;
    public:
        City(City&& other) noexcept : name(std::move(other.name)) {}
    };
    ```
    *Пример с динамической памятью:*
    ```cpp
    class City {
        char* name;
    public:
        City(City&& other) noexcept : name(other.name) {
            other.name = nullptr;
        }
    };
    ```

* **Конструктор преобразования** — позволяет неявно преобразовывать тип аргумента в тип класса.
    ```cpp
    class City {
        std::string name;
    public:
        City(const char* n) : name(n) {}
        void print() const { std::cout << name << std::endl; }
    };

    void showCity(City city) { city.print(); }

    int main() {
        showCity("Berlin"); // "Berlin" автоматически станет объектом City
        return 0;
    }
    ```

● **Деструктор** — «обратный конструктор». Выполняется автоматически при уничтожении объекта. 
* Имеет имя класса с символом `~` в начале.
* Не принимает параметров.
* Используется для очистки памяти или закрытия файлов.