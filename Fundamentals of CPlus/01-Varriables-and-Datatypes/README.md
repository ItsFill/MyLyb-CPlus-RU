============================================================================================

                                         📚**ПЕРЕМЕННЫЕ И ТИПЫ ПЕРЕМЕННЫХ**

============================================================================================



**1.	ПЕРМЕННЫЕ И ИХ ОБЪЯВЛЕНИЕ**

------------------------------------------------------------------------------------------------------------------------

Переменные объявляются по логике "Тип Имя = Значение" или "Тип Имя;" во втором случае в переменной образуется "мусор".

Тип переменной не может меняться в ходе работы.

Имя не обязательно но очень желательно что бы начиналось с буквы, не начиналось с цифр и не использовать пробелы в имени



**2.	ТИПЫ ПЕРЕМЕННЫХ**

------------------------------------------------------------------------------------------------------------------------

● char - Тип переменной для символа  |  1 байт.

● short - Тип переменной для чисел от -32 768 до 32 767 |  2 байта.

● int - Тип переменной для чисел от -2,1 миллиарда до +2,1 миллиарда | 4 байта.

● long - Тип переменной для чисел от -2,1 миллиарда до +2,1 миллиарда | от 4 байтов до 8 байтов зависит от ос.

● long long - Тип переменной для чисел от -9,2 квантилионов до +9,2 квантилионов |  8 байтов.

● float - Тип переменной для указания числа с точкой и с точностью 7 знаков после точки| 4 байтов.

● double - Тип переменной для указания числа с точкой и с точностью 15 знаков после точки| 8 байтов.

● bool - Тип переменной для указания правды или не правды| 1 байт.



**3.	МОДИФИКАТОРЫ/СПЕЦИФИКАТОРЫ**

------------------------------------------------------------------------------------------------------------------------

● unsigned - Модификатор который указывает что переменная не может хранить отрицательные значения.

  - увеличивается макс число с плюсом.

● signed - Модификатор который указывает что переменная может хранить все и отрицательные и положительные значения.

  - используется автоматом при создании переменной.

● const - Модификатор который указывает что переменная явлется не изменяемой или же константой.



