============================================================================================







&nbsp;                                               📚\*\*КОНСТРУКТОРЫ В ООП\*\*







============================================================================================







\*\*1.	КОНСТРУКТОРЫ\*\*



---



● Конструктор состоит из имени класса



● Существует 5 основных вида конструкторов:

&nbsp;  - Конструктор по умолчанию — этот конструктор ничего не делает и не принимает и создается если нет ни одного явно

&nbsp;  прописанного конструктора, конструктор является неявным

&nbsp;  - Параметризованный конструктор — это конструктор, который принимает в себя разные параметры и передает в класс

&nbsp;  например:

```class City

{

&nbsp;   std::string name;

public:

&nbsp;   City(std::string name) : name(name) {}

};
```

```class City

{

&nbsp;   std::string name;

public:

&nbsp;   City(std::string name)

&nbsp;   {

&nbsp;       this->name = name;

&nbsp;   }

};
```

&nbsp;  - Конструктор копирования — это конструктор, который внутрь себя принимает объект того же класса которым и является

&nbsp;  например:

```class City

{

&nbsp;   std::string name;

public:

&nbsp;   City(City\& city)

&nbsp;   {

&nbsp;       this->name = city->name;

&nbsp;   }

};
```

&nbsp;  - Конструктор переноса — этот конструктор схож по задумке с конструктором копирования, но с условием что он

&nbsp;  яко бы ворует данные с передаваемого объекта и уничтожает передаваемы объект, например:

```class City {

&nbsp;   std::string name;

public:

&nbsp;   City(City\&\& city) noexcept : name(std::move(city.name)) {

&nbsp;   }



&nbsp;   void print() const {

&nbsp;       if (!name.empty()) std::cout << "City: " << name << std::endl;

&nbsp;       else std::cout << "City is empty" << std::endl;

&nbsp;   }

};
```

```class City {

&nbsp;   char\* name;

public:

&nbsp;   City(char\* n) : name(n) {}



&nbsp;   City(City\&\& city) noexcept : name(city.name) {

&nbsp;       city.name = nullptr;

&nbsp;   }



&nbsp;   void print() const {

&nbsp;       if (name) std::cout << "City: " << name << std::endl;

&nbsp;       else std::cout << "City is empty" << std::endl;

&nbsp;   }



&nbsp;   ~City() {

&nbsp;       delete\[] name;

&nbsp;   }

};
```

&nbsp;  - Конструктор переобразования — этот конструктор позволяет неявно преобразовывать тип аргумента в тип класса,

&nbsp;  например:

```class City {

&nbsp;   std::string name;



public:

&nbsp;   City(const char\* n) : name(n) {}



&nbsp;   void print() const {

&nbsp;       std::cout << name << std::endl;

&nbsp;   }

};



void showCity(City city) {

&nbsp;   city.print();

}



int main() {

&nbsp;   showCity("Berlin");

&nbsp;   return 0;

}
```

● Так же существуют деструкторы, их можно назвать обратным конструктором, тоесть выполняется при уничтожении объекта

&nbsp; и объявляется один раз как и конструктор по имени, только перед именем надо напистаь символ ~, так же не принимает

&nbsp; параметров.

