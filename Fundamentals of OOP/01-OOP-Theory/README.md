========================================================================================================================



&nbsp;                                                   📚**ТЕОРИЯ ООП**



========================================================================================================================



**1.	ЧТО ТАКОЕ ООП?**

------------------------------------------------------------------------------------------------------------------------



● **Это база разработки на с++, в котором мы стараемся описать объект.**



● **Основные термины:**

&nbsp;  - Класс — это шаблон/чертёж, по которому мы сможем создавать объекты в программе.

&nbsp;  - Объект — это то что мы создаем по шаблону.

&nbsp;  - Поле — это тоже самое что и переменная, только внутри класса.

&nbsp;  - Метод — это тоже самое что и функция, только внутри класса.



● **В ООП существует 4 основных правила/кита разработки:**

&nbsp;  - Инкапсуляция — это скрытие внутри данных и предоставление контролируемого доступа через публичный интерфейс.

&nbsp;  - Наследование — это возможность создавать классы родители и потомки, то есть классы которые

&nbsp;  перенимают характеристики от другого класса.

&nbsp;  - Полиморфизм — это возможность реагировать на одно действие разным объектам по разному.

&nbsp;  - Абстракция — это создание важных для программы характеристик и действий объекта и

&nbsp;  удаление не нужных характеристик и действий.



**2. КЛАССЫ**

------------------------------------------------------------------------------------------------------------------------

● Класс — это шаблон/чертёж, по которому мы сможем создавать объекты в программе.

● **Модификаторы класса:**

&nbsp;  - Public — этот модификатор для того что бы мы могли использовать методы в мейне(Обычно здесь пишут методы)

&nbsp;  - Private — этот модификатор что бы мы могли использовать только внутри самого класса(Обычно здесь пишут поля)

&nbsp;  - Protected — этот модификатор что бы мы могли использовать внутри самого класса и его наследники


● **Конструкторы и деструкторы:**

&nbsp;  - Конструктор — это тот же метод, но он не начинается как привычная функция, он состоит из имени

&nbsp;  и вызывается при создании объекта по этому классу

&nbsp;  - Перегрузка конструктора — это два или более конструктора которые разглядывают разные способы вызова объекта

&nbsp;  - Деструктор — это тот же конструктор но помимо того что он состоит из имени перед именем пишется ~

&nbsp;  и вызывается при удалении объекта по этому классу

&nbsp;  - "this->" — используется в случае если мы имеем два одинаковых имени, например есть конструктор  

&nbsp;  который указывает год в нашем конструкторе и у нас есть поступающая переменная year и поле year

&nbsp;  и что бы это все работало и код понимал что к чему, надо писать перед **полем** this-> тем

&nbsp;  самым указывая что этот объект находится внутри класса